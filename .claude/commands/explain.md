---
description: Explain how a file, function, or flow works — for onboarding or before changing it
argument-hint: <file path, function name, or "how does X work">
---

Explain the following so someone new to this codebase could understand it: $ARGUMENTS

1. Read the relevant code (follow imports/callers as needed).
2. Start with a one-sentence summary of its purpose.
3. Walk the main flow in plain prose — inputs, what happens, outputs, side effects.
4. Note the non-obvious bits: edge cases, gotchas, why it's built this way.
5. Reference specific `file:line` locations so I can follow along.

Don't change any code. This is a read-only explanation.
